<%= simple_form_for [@episode, note], url: episode_note_path(@episode, note) do |f| %>
  <%= f.input :description%>
  <div class="form_new_validation">
    <%= f.submit "Modifier la note", class: "btn-dark-blue"%>
  </div>
<% end %>
